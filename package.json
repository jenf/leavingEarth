{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src -d lib",
    "test": "babel testsrc -d test; ./node_modules/mocha/bin/mocha"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "mocha": "^4.0.1"
  }
}
